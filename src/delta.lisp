;;;; delta.lisp — Stage 9
;;;; The trust boundary. LLM proposes a delta struct.
;;;; Kernel validates and commits — or rejects.

(in-package :genesis)

;;; TODO Stage 9: defstruct delta (verb args rationale capability)
;;; TODO Stage 9: apply-delta — check cap → validate → execute verb
