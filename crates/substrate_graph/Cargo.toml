[package]
name = "substrate_graph"
version = "0.1.0"
edition = "2021"

[dependencies]
substrate_core = { path = "../substrate_core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
